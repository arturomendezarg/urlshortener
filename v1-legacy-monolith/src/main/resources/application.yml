server:
  port: 8080

spring:
  application:
    name: v1-legacy-monolith
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:urlshortener}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    # Fail fast if the broker is unavailable, instead of blocking the redirect thread
    # waiting for the AMQP client's default timeout (see ClickEventPublisher: fire-and-forget,
    # must never prevent the redirect from responding).
    connection-timeout: 2000

app:
  messaging:
    click-events-queue: click-events

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: when-authorized
