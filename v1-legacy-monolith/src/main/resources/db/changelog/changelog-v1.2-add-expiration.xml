<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="v1.2-add-expires-at" author="art-claude-dev">
        <comment>
            Brownfield scenario (see ARCHITECTURE.md section 6, Scenario B): adds expires_at
            to the existing urls table. Deliberately NULLABLE (not NOT NULL): current rows
            have no value and NULL is interpreted as "never expires", preserving exactly the
            previous behavior for everything that already exists. No downtime, no data
            migration needed.
        </comment>
        <addColumn tableName="urls">
            <column name="expires_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
