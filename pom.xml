<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.artmendez.urlshortener</groupId>
  <artifactId>url-shortener-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>URL Shortener - Parent</name>
  <description>Prototipo de URL shortener - proyecto de ejercicio de AI-assisted engineering. Ver ARCHITECTURE.md.</description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.version>3.3.4</spring-boot.version>
  </properties>

  <!--
    Los módulos se agregan uno por uno a medida que cada tarea del plan (ARCHITECTURE.md §7) se
    implementa en su propio PR, para mantener la trazabilidad tarea-por-tarea.
  -->
  <modules>
    <module>v1-legacy-monolith</module>
    <module>api-gateway</module>
    <module>v2-shortener-contract</module>
  </modules>

  <!--
    Quality gates transversales (ver ARCHITECTURE.md sección 11 y AI_USAGE_LOG.md): declarados
    aquí como <build><plugins> (no <pluginManagement>) para que cada módulo hijo los ejecute
    automáticamente en su fase verify, sin tener que repetir la configuración.

    - checkstyle: análisis estático de estilo/errores comunes (ruleset propio en checkstyle.xml,
      deliberadamente acotado para arrancar como gate real sin generar una ola de violaciones
      sobre código ya escrito).
    - spotbugs: análisis estático de patrones de bugs sobre el bytecode compilado.

    El escaneo de dependencias (seguridad) se resolvió con GitHub Dependabot en vez de un plugin
    de Maven — ver ARCHITECTURE.md sección 11 para la justificación de ese cambio de enfoque.
  -->
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>3.3.1</version>
        <configuration>
          <configLocation>../checkstyle.xml</configLocation>
          <consoleOutput>true</consoleOutput>
          <failOnViolation>true</failOnViolation>
          <violationSeverity>error</violationSeverity>
          <includeTestSourceDirectory>true</includeTestSourceDirectory>
        </configuration>
        <executions>
          <execution>
            <id>checkstyle-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>4.8.6.4</version>
        <configuration>
          <effort>Default</effort>
          <threshold>Medium</threshold>
          <failOnError>true</failOnError>
        </configuration>
        <executions>
          <execution>
            <id>spotbugs-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
