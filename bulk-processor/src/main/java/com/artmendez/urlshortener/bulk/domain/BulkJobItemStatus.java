package com.artmendez.urlshortener.bulk.domain;

/** Mirrors v2-shortener-service's enum of the same name/values — see {@link BulkJobStatus}. */
public enum BulkJobItemStatus {
    PENDING,
    COMPLETED,
    FAILED
}
