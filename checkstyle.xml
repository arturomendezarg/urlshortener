<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Deliberately minimal, low-risk ruleset (quality gates task, see AI_USAGE_LOG.md).
  Initial goal: the gate exists and can actually fail (not a placeholder that never
  fails), without generating a wall of style violations against already-written code. It can
  be progressively hardened (javadoc, line length, naming conventions, etc.) on
  Day 2/3 as the team decides the desired level of rigor.
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>

    <module name="NewlineAtEndOfFile"/>
    <module name="FileTabCharacter"/>

    <module name="TreeWalker">
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>
        <module name="EqualsHashCode"/>
        <module name="EmptyCatchBlock"/>
        <module name="ModifierOrder"/>
    </module>
</module>
